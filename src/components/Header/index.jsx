import './header.css'

function Header(){
    return(
        <header>
            <a href="#">Usuarios</a>
            <a href="#">Cadastrar usuario</a>
            <a href="#">Cadastrar endereco</a>
        </header>
    );
};

export default Header;